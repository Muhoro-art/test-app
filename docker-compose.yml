version: '3.8'
services:
  app:
    build: .
    ports:
      - '3000:3000'
    environment:
      - PORT=3000
      - DB_URL=mongodb://db:27017/test-app
  db:
    image: mongo
    ports:
      - '27017:27017'